---

const { trnsitionProps } = Astro.props;

---

  <div class="w-full flex flex-col gap-4 items-center justify-center text-center" >
      <img class="w-120" transition:name={`${trnsitionProps.title}Img`} src={trnsitionProps.img.src} alt="">
    <h1  class="font-bold text-5xl">{trnsitionProps.title}</h1>
  </div>